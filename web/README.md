# Web

React + TypeScript 前端空壳工程，当前只保留项目结构和基础运行能力，不包含业务页面、接口封装或后端 API 调用。

## 命令

```bash
pnpm install
pnpm run dev
pnpm run build
```

默认开发地址为 `http://127.0.0.1:5173`。后端地址通过 `.env` 中的 `VITE_API_BASE_URL` 指定，示例见 `.env.example`。
