# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**2026-06-23 补充（MCP Client / Server 已实现，当前暂不认证）**：`app/mcp/` 采用“同仓库、独立入口、独立进程、独立端口”边界。`server.py` 通过 `conda run -n arch-fatapi python -m app.mcp.server` 启动无状态 Streamable HTTP Server（dev 默认 `127.0.0.1:8010/mcp`），不挂载现有 FastAPI；无认证 Server 只允许绑定 loopback，prod 默认 `mcp.server.enabled=false`。`client.py` 每次操作建立短生命周期 `ClientSession`，支持 `list_tools/call_tool/ping`、连接/调用超时和进程级并发限制，工具调用不自动重试；Client 不生成认证头。`manager.py` 启动期只注册静态 Server、不联网，单个无效配置降级跳过，已接入 FastAPI lifespan 并最先关闭。当前已完整删除 MCP 认证实现、配置和占位，未来需要时作为独立特性重新设计。`tools/demo.py::calculate` 为结构化四则运算 demo，不使用 eval；调用示例为 `conda run -n arch-fatapi python -m examples.mcp_client_demo`。MCP 配置挂 `Settings.mcp`，三套 yaml 已同步；官方 SDK 锁定 `mcp>=1.27,<2`（v2 当前仍为 alpha）；错误码使用 25xxx（25001-25006），避免与 Skill 23xxx / Milvus 24xxx 冲突。MCP 本地测试 **29 passed**；全量排除既有 `test_api_response.py` collection error 后为 **251 passed / 4 failed**，4 个失败与实现前基线一致（AccessLog 1、旧配置断言 2、LLM mock 1）；`compileall`、`pip check`、`git diff --check` 通过。

## 项目当前状态（务必先读）

**2026-06-23 补充（Embedding + 图文多模态已实现）**：`app/core/llm/` 新增 `models.py`（厂商无关 `LLMUsage` / `ImageInput` / `EmbeddingResponse` / `MultimodalResponse`）、`embedding.py`（`embed()` 批量文本向量化）与 `multimodal.py`（`analyze_images()` URL/Base64 多图理解）；两项能力复用 `gateway.py` 启动期缓存的 `AsyncOpenAI` 和 `provider_configs`，请求期不新建客户端。`LlmProviderConfig` 新增可选 `embedding_model` / `multimodal_model`，为空即未声明能力；三套 yaml 的 OpenAI 默认配置 `text-embedding-3-small` / `gpt-4o-mini`。Embedding 支持单批最多 2048 条、批量顺序恢复、模型覆盖、可选 dimensions、usage 归一化；图文支持最多 10 张 HTTPS URL 或 Base64 Data URL、`auto/low/high` detail，单图解码后最大 10 MiB，校验 JPEG/PNG/WEBP/GIF 魔数且拒绝动画 GIF，服务端不下载远程图片。新增错误码 `LLM_ERRNO_CAPABILITY_NOT_CONFIGURED=21005`、`LLM_ERRNO_INVALID_INPUT=21006`，厂商调用/响应异常继续用 21003/21004；验证接口为 `POST /v1/llm/embeddings/test`、`POST /v1/llm/multimodal/test`。本地新增 42 个 Mock 测试覆盖公共契约、启动期配置缓存、调用、输入安全与 API，不连真实云端。

**2026-06-19 补充（LLM 当前实现以此为准）**：`app/core/llm/gateway.py` 当前采用 `openai.AsyncOpenAI` 直接缓存客户端实例（`llm_instances`）+ `langchain_models` 缓存 LangChain ChatModel；已去除对不存在的 `base.py` / `openai_provider.py` 源码依赖。密钥仍统一经 `LLM_API_KEY_<大写PROVIDER>` 注入，由 `gateway.py` 读取（优先系统环境变量，兜底项目根 `.env`）；配置已新增智谱 `zhipu` Provider（OpenAI 兼容端点 `https://open.bigmodel.cn/api/paas/v4/`，默认模型 `glm-4-flash`），真实 key 不写入 yaml / 代码；新增测试接口 `GET /v1/llm/zhipu/test`，用于验证智谱连通性，401 时优先检查 `LLM_API_KEY_ZHIPU` 是否正确并重启服务让启动期 `init_llm()` 重新建客户端；另有 `GET /v1/llm/langchain/test?provider=zhipu` 直接通过 `get_langchain_llm()` 调用 LangChain ChatModel，便于单独验证 LangChain 层；LangChain 的 OpenAI 兼容模型需要 `langchain-openai` 包，已加入 `requirements.txt`，缺失时按 `pip install -r requirements.txt` 补齐；LangSmith tracing 开关在 `configs/*.yaml` 的 `llm.langsmith.enabled`（默认 `false`=零上报，避免本地/测试自动上报到 `api.smith.langchain.com` 产生 403）——`gateway.py` 的 `configure_langsmith_tracing` / `langchain_tracing_context` 两函数按此开关控制（此前被引用却未定义，致 `import llm_router` 报 `ImportError`，已补齐修复）；**开关关时强制把 `LANGSMITH_TRACING_V2` / `LANGCHAIN_TRACING_V2` 设 `false`，压制外部 shell/IDE 注入的 `true`+占位 key**（否则 langchain 运行时读环境变量仍上报致 403——这是「开关关不能只不设、必须主动压」的关键，已实测 `_tracing_v2_is_enabled()=False` 验证）；启用时把 yaml `enabled` 改 `true` 并配 `LANGSMITH_API_KEY` 环境变量，project 取 yaml `llm.langsmith.project`。

**2026-06-19 补充（Skill 注册中心已实现）**：`app/core/skills/` 为注册中心引擎（`models.py`/`loader.py`/`registry.py`），`app/skills/<name>/SKILL.md` 为数据资产；启动期 `init_skills(settings.skills)` 扫描 `base_dir/*/SKILL.md` 建 `skill_index` 轻量索引，请求期 `load(name)` 懒加载正文组装 `SkillBundle` 并按 `cache_loaded` 缓存。扫描候选目录时会静默忽略 `__pycache__` 和隐藏目录，避免测试/启动后 Python 运行时产物触发“skill 目录缺少 SKILL.md”噪音。已接入 `app.server.lifespan`（LLM 之后初始化，shutdown 清缓存）和 `app/api/routes.py`，提供 `GET /v1/skills`、`GET /v1/skills/{name}`、`POST /v1/skills/{name}/run`；执行入口由 `SkillService` 加载正文作 `SystemMessage`、用户输入作 `HumanMessage`，经既有 `get_langchain_llm()` 调用，继续遵守「LLM 访问收敛 core/llm」约定。`SkillSettings` 挂在 `Settings.skills`，三套 yaml 均有 `skills.enabled/base_dir/cache_loaded`；当前项目 env_prefix 为空，环境覆盖用 `SKILLS__BASE_DIR` / `SKILLS__ENABLED` / `SKILLS__CACHE_LOADED`。错误码新增 23xxx 子段：`SKILL_ERRNO_NOT_INITIALIZED=23001`、`SKILL_ERRNO_NOT_FOUND=23002`（HTTP 404）、`SKILL_ERRNO_LOAD_FAILED=23003`、`SKILL_ERRNO_DISABLED=23004`、`SKILL_ERRNO_RUN_FAILED=23005`。内置 demo skill：`app/skills/summarize/SKILL.md`。

**2026-06-23 补充（Milvus 企业级向量库访问封装已实现）**：`app/core/milvus.py` 基于 `pymilvus.AsyncMilvusClient` 管理进程级异步客户端，启动时通过 `list_collections` 做只读连通性探测，失败 warning + 清空客户端降级运行，不阻断应用；`get_milvus()` 供业务严格获取，`get_milvus_optional()` 供 readyz 观测，`close_milvus()` 幂等关闭。`app/repositories/vector.py` 的 `VectorRepository(collection_name)` 提供 collection create/drop/has/load/release、insert/upsert 自动分批、delete/get/query/search；写操作不自动重试，避免重复或部分写语义不清，search 归一为 `app/schemas/vector.py::VectorSearchHit`，上层不接触 SDK 返回对象。`MilvusSettings` 已挂 `Settings.milvus`，三套 yaml 含 enabled/uri/db_name/timeout/batch_size，token 仅经 `MILVUS__TOKEN` 注入；readyz 并发检查 DB/Redis/Milvus，Milvus 不可用标 degraded 但仍返回 200。错误码使用 24xxx：24001 未初始化、24002 连接失败、24003 操作失败、24004 响应异常。

**配置中心 + 启动链 + 全局异常捕获 + 生产级结构化日志 + 全局异步 HTTP 封装 + 跨域中间件 + RequestID 请求追踪 + 访问日志中间件（AccessLog 接管 uvicorn access log，带 request_id + 耗时） + SQLAlchemy 数据层 + Redis 缓存层 + Milvus 向量库访问层 + 业务分层案例（user：api→schemas→services→repositories 全链路）+ LLM 网关层（openai SDK 统一接入 OpenAI/DeepSeek/Qwen/Claude，chat/streaming/tool_calling/usage）+ Skill 注册中心（SKILL.md 扫描索引/懒加载/统一执行入口）+ MCP Client / 独立 Server 已实现**：

- **配置中心**：`app/core/config.py`（pydantic-settings + yaml + .env + 环境变量多源，仅承载 `Settings` 根配置 + 多源加载 + `get_settings`，带 yaml 缺失 fail-fast；configs 目录经 `_resolve_configs_dir()` 解析——可被环境变量 `APP_CONFIG_DIR` 覆盖（容器/生产挂载入口），项目根用 `.git`/`requirements.txt` 标记向上查找抗文件移位；对外仍 re-export `AppSettings`，引用路径不变）；**配置段模型** `app/core/settings/`（schema 层，所有 `XxxSettings` 集中在 `settings.py` 单文件定义，避免 config.py 臃肿（当前已含 App/Logging/DB/Redis/Milvus/Cors/Llm/Skill/Mcp 九段；仅 HTTP 客户端参数与 RequestID 策略写死不进配置）；新增配置段时在 `settings.py` 加 class + `__init__` 导出 + `Settings` 根聚合字段）；`configs/{dev,test,prod}.yaml`、`.env.example`、`.gitignore` 就位。
- **启动链已打通**：`main.py`（FastAPI 入口）→ `app/server.py`（启动期依次初始化 DB/Redis/Milvus/LLM/Skill/MCP Clients，关闭期按逆序尽力释放）；独立 MCP Server 使用另一入口 `python -m app.mcp.server`，不进入 FastAPI lifespan、不共享监听端口或故障域。业务路由由 `app/api/routes.py` 聚合。
- **全局异常捕获 + 统一响应规范已实现**：`app/utils/common.py`（`ApiResponse{code,message,data,errno?}`，`ok()`/`fail()` + `to_payload()`——data 始终输出、errno 仅业务异常输出，故不能用 `exclude_none`）；`app/exceptions/base.py`（`BizException` 基类 + `BizNotFoundError`/`BizAuthError`/`BizForbiddenError`/`BizValidationError` 四子类，`status_code`/`errno`/`message` 为类属性，构造可覆盖）；`app/exceptions/handlers.py`（四 handler：`BizException`→INFO、`StarletteHTTPException`+`FastAPIHTTPException` **双注册**覆盖默认（含路由 404，易错点）→WARNING、`RequestValidationError`→INFO（字段级错误入 data）、`Exception` 兜底→ERROR+堆栈按 `app.debug` 脱敏）；`app/server.py` 的 `create_app` 按「路由→异常→中间件」顺序注册。**code 复用 HTTP 状态码，业务细码走 `errno`**。注意：测 500 兜底 handler 时 `TestClient` 须 `raise_server_exceptions=False`（ServerErrorMiddleware 调 handler 后仍 re-raise）。**errno 类型契约（严禁违反）**：errno 全系统统一为 **`int`**（`BizException.errno: int` + `ApiResponse.errno: Optional[int]` + 4 子类全是 int）——**严禁传字符串**，否则同一项目响应里 errno 时而数字时而字符串、前端类型不稳定（早期 `database.py` 曾误用字符串如 `"DB_CONNECT_FAILED"`，已统一改正）。编码分段：HTTP 类异常走 `1`+HTTP码（404→10404）、业务自定义走 `1xxxx` 段、**基础设施层故障（DB/Redis/LLM/ES）走 `2xxxx` 段**（集中定义在 `app/exceptions/base.py` 末尾常量，避免魔法数字散落；按子类分段互不混淆——DB 用 20xxx、LLM 用 21xxx、Redis 未来用 22xxx、Skill 用 23xxx）。当前 DB 段：`DB_ERRNO_ENGINE_CREATE_FAILED`=20001、`DB_ERRNO_CONNECT_FAILED`=20002、`DB_ERRNO_QUERY_FAILED`=20003、`DB_ERRNO_DISPOSE_FAILED`=20004、`DB_ERRNO_NOT_INITIALIZED`=20005；**LLM 段**：`LLM_ERRNO_NOT_INITIALIZED`=21001、`LLM_ERRNO_PROVIDER_NOT_FOUND`=21002、`LLM_ERRNO_CALL_FAILED`=21003、`LLM_ERRNO_INVALID_RESPONSE`=21004；**Skill 段**：`SKILL_ERRNO_NOT_INITIALIZED`=23001、`SKILL_ERRNO_NOT_FOUND`=23002、`SKILL_ERRNO_LOAD_FAILED`=23003、`SKILL_ERRNO_DISABLED`=23004、`SKILL_ERRNO_RUN_FAILED`=23005，均经 `app/exceptions/__init__` 导出供 `database.py`/`repositories/dao/*.py`/`core/llm/`/`core/skills/` 引用；新增基础设施错误码在 base.py 追加常量。
- **生产级结构化日志已实现（全环境人类可读单行，去 JSON 简化）**：`setup_logging(LoggingSettings)` 配 3 sink（stdout + `logs/app.log` 全量 + `logs/error.log` ERROR 分流），**全环境统一人类可读单行**（`_DEFAULT_FORMAT`：时间｜级别｜模块:函数:行号｜`[req_id=xxx]`｜消息；stdout 彩色、文件纯文本颜色自动剥离）——项目不接外部日志采集，prod 也用可读文本而非 JSON（YAGNI；弃用此前 patcher 预生成精简 JSON + format 引用 `{extra[__json__]}` 的绕圈机制，`LoggingSettings.serialize` 配置项一并移除），enqueue 多进程安全、按 rotation/retention/compression 轮转，patcher 仅注入 env/service/request_id 三字段（request_id 从 `app.middleware.request_id.get_request_id()` 读取，请求上下文外为 `-`）；配置段 `LoggingSettings`（level/dir/rotation/retention/compression/diagnose/backtrace/enqueue，diagnose dev 开/prod 关防泄露），`app.server.load_config` 传 settings.logging。**第三方库噪音治理（修复 dev 被 httpx/openai 协议日志刷屏）**：标准库 `logging` 与 loguru 是两套独立体系——`intercept_uvicorn_logs` 经 `logging.basicConfig(force=True)` 把标准库**根 logger** 接管进 loguru，根级别取「**不低于 INFO**」（`max(getattr(logging, level.upper(), INFO), INFO)`）**不再跟随 `app.log_level`**：此前 dev 下 `app.log_level=DEBUG` 致根 logger=DEBUG，httpx/httpcore/openai（默认 NOTSET 继承根）把 `receive_response_body.complete` / `response_closed.started` / `response_closed.complete` 等 HTTP 生命周期 DEBUG 事件全经 `InterceptHandler` 桥接进 loguru 刷屏（日志来源显示 `logging:callHandlers` 即标准库派发痕迹）；另 `_silence_noisy_loggers()` 把已知噪音库（`_NOISY_LOGGERS`：httpx/httpcore/openai/openai._base_client/asyncio/urllib3/urllib3.connectionpool/charset_normalizer）**显式提级 WARNING**——出站 HTTP 客户端库只该暴露失败/重试/超时，协议细节滤掉，由 `setup_logging` 在 `logger.remove()` 后调用（与根级别双保险）。**应用 loguru 日志级别不受影响**（dev 仍 DEBUG），排查 LLM 时若需临时看 httpx 细节，调用处 `logging.getLogger("httpx").setLevel("DEBUG")` 即可，调试完即撤。
- **全局异步 HTTP 封装已实现**：`app/utils/http_client.py`（基于 `httpx.AsyncClient` 的进程级单例封装：`get_client()` 惰性创建复用连接池，禁止每次 new client 致连接泄漏；超时 `httpx.Timeout` 四阶段（connect/read/write/pool）+ 连接池 `httpx.Limits`（max_connections/max_keepalive/keepalive_expiry）+ TLS verify + 默认 UA + 重试策略均以**模块级常量写死**（不进配置，将来需按环境调优再改配置驱动）；**重试三条件同时满足**才重试——① `httpx.TransportError` 网络异常或状态码命中 `retry_on_status` ② 幂等方法（GET/HEAD/OPTIONS/PUT/DELETE）③ 未超 `max_retries`，指数退避 `factor*2**n`，非幂等 POST/PATCH 一律不重试；失败统一 `raise BizException(...) from exc`（零裸 Exception）由全局 handler 转统一响应；loguru 记请求/响应/重试。便捷方法 `get/post/put/delete/patch` + `request()` 核心入口；`close_client()` 已接入 `app.server.lifespan`，统一清理器按 Skills → LLM → Milvus → HTTP → DB → Redis 顺序尽力释放，单项失败不阻断后续资源关闭）。
- **跨域中间件已实现（配置驱动，dev/test 全放行 / prod 收敛白名单）**：`app/middleware/cors.py`（`setup_cors(app, cors)` 按传入的 `CorsSettings` 注册 `starlette CORSMiddleware`，策略各环境可异——dev/test yaml 配 `allow_origins=["*"]` 全放行（开发友好）、prod yaml 配明确域名 + REST 方法 + 头部白名单（安全合规）；`allow_credentials` 固定 False——JWT 走 Authorization 头无 cookie 凭证，且规避 `["*"]+True` 非法组合）；配置段 `CorsSettings`（allow_origins/allow_methods/allow_headers/allow_credentials/expose_headers/max_age，挂 `Settings.cors`，env 覆盖 `APP__CORS__*`，**list 字段须 JSON 数组格式**如 `APP__CORS__ALLOW_ORIGINS=["https://a.com"]`）；`app.server.register_middlewares` 调 `setup_cors(app, settings.cors)`（CORS 作为「路由→异常→中间件」最后注册，异常 handler 优先）。prod origins 真值走环境变量注入，yaml 留占位 `https://example.com`；prod 头白名单含 `X-Request-Id`（入站）且 `expose_headers=[X-Request-Id]`（出站，追踪链闭环）。
- **RequestID 请求追踪中间件已实现（策略写死，不进配置）**：`app/middleware/request_id.py`（`setup_request_id(app)` 注册 `RequestIDMiddleware`；入站从 `X-Request-Id` 头读取或用 `uuid.uuid4().hex` 生成（32 字符无连字符），存入 `contextvars.ContextVar` 供请求生命周期访问；出站响应头回写 `X-Request-Id`；提供 `get_request_id()` 函数读取当前请求 id）；`app.server.register_middlewares` 调 `setup_request_id(app)`（在 CORS 之后、业务中间件之前注册，确保日志尽早带 id）。日志已集成 request_id（dev 格式 `[req_id=xxx]`，prod JSON 含 `request_id` 字段）。
- **访问日志中间件已实现（AccessLog，接管 uvicorn access log）**：`app/middleware/access_log.py`（`setup_access_log(app)` 注册 `AccessLogMiddleware`；`call_next` 前后用 `time.perf_counter` 计时，`finally` 打访问日志——`{client} - "{method} {path} HTTP/1.1" {status} {duration_ms:.1f}ms`，request_id 由 logger patcher 注入，含请求耗时）。**为何自写而非用 uvicorn 自带 access log**：uvicorn access log 在 ASGI app 全部中间件返回后才打，此刻 `RequestIDMiddleware` 已在 `finally` 把 request_id contextvar reset 掉，patcher 注入恒为 `-`（本次修复的根因）；自写中间件把打印时机移到 `call_next` 返回后、contextvar 仍活跃的区间，故能带真实 id。**注册顺序关键**：必须在 RequestID **内层**（`add_middleware` 调用早于 `setup_request_id`——starlette 后 add 者更外层，RequestID 最外层 set id 后本中间件 dispatch 才读得到）。配套 `AppSettings.access_log` 默认改 `False`（`server.py` 传 uvicorn），关闭 uvicorn 自带访问日志避免重复。`app.server.register_middlewares` 顺序：`setup_cors`（最内）→ `setup_access_log` → `setup_request_id`（最外）。
- **依赖**：`requirements.txt` 已含 `fastapi` / `uvicorn[standard]` / `loguru` / `httpx` / `redis` / `fakeredis` / `pymilvus>=2.5,<3.0`（Milvus `AsyncMilvusClient`）；CORS 用 FastAPI/starlette 自带 CORSMiddleware，无需新依赖。
- **Redis 异步缓存层已实现**：`app/core/redis.py`（redis.asyncio 进程级单例，`get_redis()` 惰性创建复用连接池（降级/未初始化时抛 `RuntimeError`，cache 层 fail-loud 需要），另提供 `get_redis_optional()` 返回 `Optional[Redis]` **不抛异常**——供 readyz 等只读探针区分「在线」与「降级运行」（探针须把 Redis 不可用判为 degraded 而非失败，故不能用抛异常版）；`init_redis(settings.redis)` / `close_redis()` 接入 `app.server.lifespan`；**连接失败降级**：build/ping 失败不 fail-fast、记 warning + 清空单例降级运行，Redis 属非核心依赖）、`app/utils/cache.py`（cache-aside 访问封装：`cache_get/cache_set/cache_delete` + JSON 序列化 + TTL、`get_or_set` 工厂模式；**归 utils 层**——与 `http_client.py` 同性质的泛型基础设施工具，不绑业务实体，故不在 repositories 下）、配置段 `RedisSettings`（url/db/max_connections/decode_responses/encoding）、lifespan 启动/关闭已接入、`tests/test_redis.py`（17 个测试，用 fakeredis 模拟，含 2 个 Redis 失败路径回归测试，全过）。
- **Milvus 异步向量库访问层已实现**：`app/core/milvus.py` 管理 `AsyncMilvusClient` 单例、只读启动探测、严格/可选获取与幂等关闭；Milvus 非核心依赖，连接失败降级运行。`app/repositories/vector.py` 的 `VectorRepository` 绑定 collection，覆盖 create/drop/has/load/release、insert/upsert 分批、delete/get/query/search；写失败不自动重试，search 严格归一为 `VectorSearchHit{id,distance,entity}`。配置段 `MilvusSettings`（enabled/uri/token/db_name/timeout/batch_size）挂根配置，token 仅走 `MILVUS__TOKEN`；SDK 异常统一转 24xxx `BizException`，日志不输出 token、实体或向量。
- **SQLAlchemy 异步数据层已实现**：`app/core/database.py`（进程级 async engine 单例 + `async_sessionmaker`，`await init_db(settings.db)` **异步初始化 + SELECT 1 连通性预检 fail-fast**——DB 是核心依赖，启动期验证可达、连不上 `raise BizException(errno=DB_CONNECT_FAILED)` 拒启动，与 Redis/Milvus「非核心降级」对照；`await dispose_db()` 释放接入 `app.server.lifespan`，`get_db()` 依赖注入 yield `AsyncSession`，SQLite 测试走 NullPool）、`app/repositories/base.py`（DeclarativeBase 声明式基类 + Repository 基类）、配置段 `DBSettings`、驱动 asyncpg；ES 访问待补。
- **业务分层案例已实现（user 全链路：api → schemas → services → repositories/{models,dao}）**：以 PostgreSQL `user` 表为样本，打通「Controller → Schema → Service → DAO → Model」标准分层，作为后续业务模块的范式。**字段以库 schema 为唯一事实来源**（经 `information_schema` 实测：`id`(integer, NOT NULL, **非自增**——库里非 serial，故 ORM 不设 autoincrement，插入须显式给 id)、`user_name`(varchar(255), nullable)）。各层职责：① `app/repositories/models/user.py`（`User` ORM 模型，继承 `base.Base`，SQLAlchemy 2.0 `Mapped` 语法；`models/__init__` 导出 `Base`+`User`，一表一文件）② `app/repositories/dao/user.py`（`UserRepository`——构造注入 `AsyncSession`，纯数据访问零业务逻辑；`get_by_id`/`get_by_username`/`list_users(limit,offset)→(rows,total)`；查不到返回 `None` 把判断权交 service，DB 异常 `raise BizException(errno=DB_QUERY_FAILED)`；`dao/__init__` 导出 `UserRepository` 供 service `from app.repositories.dao import UserRepository`）③ `app/schemas/user.py`（Pydantic v2 `UserOut`/`UserListItem`/`UserListData`，`from_attributes=True` 支持 ORM→schema 直转）④ `app/services/user_service.py`（`UserService`——编排：调 repo 取数 + 业务判断（None→`BizNotFoundError`）+ ORM→schema 转换 + 分页元信息拼装）⑤ `app/api/v1/users_router.py`（路由：`Depends(get_db)` 注入 session → `UserService(db)` → `ApiResponse.ok().to_payload()`；`GET /v1/users`（分页 Query limit/offset 带 ge/le 约束）、`GET /v1/users/{user_id}`（404 由 service 抛 + 全局 handler 转））⑥ 无 `v1/__init__.py` 聚合层（v1 为 namespace package），由 `app/api/routes.py` 的 `register_routes` 直接 `from app.api.v1.users_router import router as users_router` + `app.include_router(users_router, prefix="/v1")` 挂载（符合「路由写法规范」）。**repositories 子目录约定**：`models/`（被访问的数据：ORM 实体）与 `dao/`（访问者：Repository）并列，二者互不交叉、依赖单向（dao→models）；`base.py`（Base 声明基类）留 `repositories/` 顶层；Redis 缓存属基础设施工具，归 `utils/cache.py` 不入此层。**测试**：`tests/test_user_api.py`（5 个，sqlite in-memory + `StaticPool` 共享内存库 + httpx `ASGITransport` 端到端，验证列表/分页/详情/404/参数校验；**为何 StaticPool 而非生产 NullPool**——`:memory:` 在 NullPool 下每连接独立内存库，建表连接与查询连接互不可见，测试用 StaticPool 复用单连接方可持久；生产代码 `database.py` 未动）。
- **LLM 网关层已实现（当前结构以顶部 2026-06-23 补充为准）**：`app/core/llm/gateway.py` 启动期缓存原生 `AsyncOpenAI`、Provider 配置与 LangChain ChatModel；业务能力统一复用缓存。当前支持 chat、LangChain、Embedding、图文多模态及 usage 归一化；密钥统一走 `LLM_API_KEY_<大写NAME>`，Provider 可分别用 `default_model` / `embedding_model` / `multimodal_model` 声明能力模型。单个 Provider 初始化失败降级运行，不在启动期对付费云端做 ping。
- **Skill 注册中心已实现（Anthropic Agent Skills 风格能力包）**：`app/core/skills/`（`SkillMeta` / `SkillBundle`、`parse_skill_file()`、`init_skills()` / `get_meta()` / `load()` / `list_metadatas()` / `close_skills()`）负责扫描、索引、懒加载和缓存；`app/skills/` 是数据资产目录，新增 skill 只需建 `app/skills/<name>/SKILL.md`（YAML frontmatter + markdown 正文）。registry 属非核心依赖，`enabled=false`、目录不存在、单个坏 skill 均 warning + 空运行/跳过，不阻断启动；`__pycache__` / 隐藏目录被静默忽略，不算坏 skill；`load()` 只负责返回 `SkillBundle`，不直接绑定 LLM。API 层新增 `app/api/v1/skills_router.py`：`GET /v1/skills`、`GET /v1/skills/{name}`、`POST /v1/skills/{name}/run`；`app/services/skill_service.py` 在 run 端点把 skill 正文作为 system 指令并通过 `get_langchain_llm(provider).ainvoke(...)` 执行。配置段 `SkillSettings` 已挂 `Settings.skills`，三套 yaml 已加 `skills` 段，环境覆盖用根字段形式 `SKILLS__BASE_DIR` 等。
- **测试**：`tests/` 已新增 skill 三件套：`test_skill_loader.py`(4，frontmatter/body 解析与坏格式错误码)、`test_skill_registry.py`(8，扫描建索引、懒加载缓存、关闭/缺目录降级、坏 skill 跳过、not found/disabled/name 不一致)、`test_skill_api.py`(4，列表/详情/run 端到端、not found 404、disabled、未初始化)。本次聚焦验证：`conda run -n arch-fatapi python -m py_compile ...` 通过；`conda run -n arch-fatapi python -m pytest tests/test_skill_loader.py tests/test_skill_registry.py tests/test_skill_api.py -v` 为 **16 passed**；相邻回归 `tests/test_config.py tests/test_routes.py tests/test_app.py tests/test_llm.py tests/test_skill_*.py` 为 **30 passed / 3 failed**。其中 3 个失败均为既有漂移：`test_config.py` 2 个（`AppSettings.name` 默认 `arch-fastapi111`、dev port 实际 8003 但测试仍期望 8002），`test_llm.py::test_init_llm_registers_instances` 1 个（当前 LangChain 构造也会触发 openai 客户端构造，旧测试仍假设 `AsyncOpenAI` 只调用 2 次）。旧有全量问题仍包括 `test_access_log_middleware.py::test_access_log_has_structured_fields`（断言 access log 应 bind 结构化字段 event/method 等，当前 access_log 仅打 message 字符串——属「业务字段进日志」待补缺口），以及 `test_api_response.py` 从旧路径 `app.schemas.common` 导入 `ApiResponse` 的 collection error；`test_logging.py` 已随「全可读去 JSON」重构对齐（4 测全过）。跑全量时需先修旧测试或临时 ignore。
- **Milvus 测试（2026-06-23）**：新增本地 `tests/test_milvus.py` **36 passed**，覆盖配置/错误码、客户端构造参数、单例/禁用/连接失败降级/关闭、collection 管理、SDK 异常转换与敏感异常文本不入日志、delete 选择器、insert/upsert 分批与严格计数、search 分组归一与畸形响应。Milvus + health + app + config 相邻回归排除两条已知旧配置断言后 **74 passed / 2 deselected**。全量用 `--ignore=tests/test_api_response.py` 为 **179 passed / 4 failed**，失败集合与实现前基线完全一致：AccessLog 结构字段 1、旧配置断言 2、LLM mock 调用次数 1；`test_api_response.py` 旧导入路径仍为既有 collection error。`python -m compileall -q app`、`pip check`、`git diff --check` 均通过。
- **文档约定**：代码注释一律中文（命名仍英文，见「关键实现约定」）。
- **Hook**：`.claude/` 下 Stop hook——`app/` / `main.py` / `tests/*.py` 有改动但 `CLAUDE.md` 未同步时，强制阻止回合结束（详见 `.claude/hooks/sync_claude_md.sh`）。

**开发环境为 Conda 环境 `arch-fatapi`（Python 3.11.15）**，具体命令见下文「开发命令」。

**下一步**：横向支撑层与数据层待补——`middleware/`（剩 JWT / 限流）、`core/llm/`（待补 fallback / pricing / 音频 / 图片生成）、`core/skills/`（待补热重载 / 资源文件注入 / 语义检索 / 权限配额）、`mcp/`（待补 LLM/Agent 工具注入、指标与熔断）、`repositories/`（待补具体 RAG collection Repository 与 ES）、`tasks/`（Celery / Arq 异步任务）。新增 FastAPI 生命周期注册点集中在 `app/server.py`，独立 MCP Server 保持 `app/mcp/server.py` 入口，配置统一经 `core/config.py` 读取。

## 目标架构（AI Agent 取向的 FastAPI 后端，来自 README）

采用**分层架构**，依赖自上而下单向流动，禁止反向依赖（如 repository 不得 import service）：

```
api/        Controller 层：FastAPI 路由、参数校验、结果返回
  └─ services/   业务编排层：编排下面的能力模块
       ├─ agents/      ReAct / Planner / Tool Agent
       ├─ workflows/   LangGraph / 状态机 / 多 Agent 协作
       ├─ skills/      业务专家经验沉淀与复用
       ├─ rag/         检索 / 召回 / 重排 / 知识库管理
       ├─ memory/      对话记忆 / 用户画像 / Checkpoint
       └─ mcp/         MCP Client / Server 管理
  ├─ schemas/        Pydantic 请求/响应模型
  └─ repositories/   DAO 层：PostgreSQL / Redis / Milvus / ES 访问封装
```

横向支撑层：
- `core/config.py` — 配置中心，按环境加载 `configs/{dev,test,prod}.yaml`，业务代码不硬编码配置。
- `core/logger.py` — Loguru 结构化日志。
- `core/llm/` — LLM 统一抽象（OpenAI / Qwen / DeepSeek 等），**屏蔽厂商差异**；业务层一律走此处，不直接调厂商 SDK。
- `core/prompt/` — Prompt 模板加载与版本管理。
- `middleware/` — JWT 认证、TraceId、日志、限流。
- `tasks/` — 异步任务（Celery / Arq / 定时任务）。

## 关键实现约定

- **启动链顺序**：`main.py`（factory 模式入口，`uvicorn app.server:create_app --factory`）→ `app/server.py`（集中 create_app、lifespan、run；直接调用 `app/api/routes.py` 聚合层并注册异常处理器、中间件）。新增启动与横切注册点统一放 `app/server.py`，**业务路由清单**集中在 `app/api/routes.py`。
- **路由写法规范（新增业务路由一律照此）**：
  - 业务路由模块放 `app/api/vN/<biz>_router.py`（文件名带 `_router` 后缀），文件内 `router = APIRouter(prefix="/<biz>", tags=["<biz>"])`，端点用 `@router.get/post/...`；
  - `app/api/vN/` 是 **namespace package，不放 `__init__.py`**（不再用 `register_vN_routes` 聚合函数，已弃用）；
  - 在 `app/api/routes.py` 的 `register_routes(app)` 内**直接**挂载：`from app.api.vN.<biz>_router import router as <biz>_router` 后 `app.include_router(<biz>_router, prefix="/vN")`，版本前缀 `/vN` 统一在此处加（模块内 router 不带 `/vN`）；
  - 基础设施端点（`/health` 等无版本前缀）仍直接 `app.include_router(<infra>_router)`；
  - **统一响应**：路由返回 `ApiResponse.ok(data).to_payload()`，业务异常由 service 抛 `BizXxxError`、全局 handler 转响应，路由内不 try/except；
  - **import 别冗余**：只写一行 `from app.api.vN.<biz>_router import router as <biz>_router`（不要多写 `from app.api.vN import <biz>_router`，那是导入模块、会被覆盖且无意义）。
- **统一响应 + 全局异常捕获**：所有 API 出口（路由与异常 handler）统一走 `app/schemas/common.py` 的 `ApiResponse{code,message,data,errno?}`（`code` 复用 HTTP 状态码；`errno` 仅业务异常输出）；业务异常 `raise app.exceptions.BizXxxError`（禁止裸 `raise Exception`），由 `app/exceptions/handlers.py` 四个 handler 统一转响应；500 兜底按 `app.debug` 脱敏（生产固定「服务内部错误」，堆栈只进日志）。新增业务异常子类在 `app/exceptions/base.py` 加 class（覆盖 status_code/errno/message）。
- **配置驱动**：按环境区分，配置文件放 `configs/`，由 `core/config.py` 统一加载。
- **分层依赖单向**：`api → services → {agents/rag/skills/workflows/memory/mcp} → repositories`，跨层调用禁止逆向。
- **LLM 访问收敛**：所有模型调用经 `core/llm/`，便于切换厂商与统一计费/限流。
- **代码注释用中文**：所有新增代码的注释一律使用中文，包括模块 / 类 / 函数的 docstring 与行内说明；变量、函数、类等命名仍遵循 PEP 8 用英文。
- **Git 提交信息用中文**：所有 commit 的标题与正文一律用中文描述，不写英文 message；类型前缀用中文动词（新增 / 修复 / 重构 / 文档 / 配置 / 测试 / 杂项）替代英文 conventional 前缀（feat / fix / refactor / docs / chore / test），例如写「新增：配置中心多源加载」而非「feat(config): add multi-source loading」。
- **tests/ 目录按约定不入库（无需反复提及）**：`.gitignore` 已忽略整个 `tests/` 目录，这是**明确的项目约定**，不是误操作——测试代码只作为本地资产存在、不进版本库。故：① `git status`/提交时看不到 `tests/*.py` 是预期行为，**不要**把"测试文件未入库"当作异常去提醒或建议改 `.gitignore`；② 改动 `app/` 代码时配套写的本地测试照常跑（conda 环境），CLAUDE.md 里记录的测试数量反映的是**本地实际状态**，与 git 里是否存在测试文件无关；③ 提交只含 `app/` 生产代码 + 文档，测试文件不 `git add`。

## 开发命令

**开发环境**：项目使用 Conda 虚拟环境 **`arch-fatapi`**（环境名如此，确实少一个 `t`，不是笔误；Python 3.11.15）。所有命令须在此环境内执行；旧的 `.venv` 已弃用、可删除。

```bash
conda run -n arch-fatapi pip install -r requirements.txt   # 安装依赖
conda run -n arch-fatapi python -m pytest tests/ -v        # 运行测试（须用 python -m，bin/pytest 入口找不到 app 包）
APP_ENV=dev conda run -n arch-fatapi python -c "from app.core.config import get_settings; print(get_settings().app.model_dump())"  # 查看生效配置
```

环境切换：`APP_ENV={dev,test,prod}` 选 yaml；configs 目录覆盖：环境变量 `APP_CONFIG_DIR`（默认 `<项目根>/configs`，容器/生产挂载到 `/etc/<app>` 等位置时设置）；敏感项覆盖：环境变量 `APP__<FIELD>` 或 `.env`（优先级 env > .env > yaml > 默认）。开发启动：`python main.py`（factory 模式，dev 自动 reload）或 `uvicorn "app.server:create_app" --factory --reload`。

## 生产部署

`app.server.run()` 配置驱动：开发（`env=dev`）自动 reload 单进程，生产（`env=prod`）按 `AppSettings` 调优参数起多 worker（`workers`/`loop`/`limit_concurrency`/`timeout_keep_alive`/`access_log`，见 `configs/prod.yaml`）。两种部署方式：

```bash
# 方式一：直接用 main.py（调优参数已在 prod.yaml 配好）
APP_ENV=prod python main.py
# 方式二：gunicorn + UvicornWorker 外置进程管理（滚动更新更成熟，容器化推荐）
gunicorn "app.server:create_app" -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000 --factory
```

生产建议 `pip install uvloop`（`loop: auto` 自动启用，比 asyncio 快 2-4 倍；Windows 不支持则自动回退）。由 systemd / docker / k8s 负责重启与滚动更新；`APP_ENV=prod` 务必 `debug=false`、`log_level` 不过度冗长。uvicorn 日志经 `intercept_uvicorn_logs` 接入 loguru，与应用日志格式统一。
